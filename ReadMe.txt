# use Qtdesigner to modify GUI_template.ui (the interface of GUI)
# the command to compile the interface of GUI
pyuic5 -x GUI_template.ui -o GUI_template.py
# the command to run GUI
python main.py